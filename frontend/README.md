This is the Next.js frontend for the Support Agent Training Simulator.

See the [project root README](../README.md) for setup instructions (both backend and frontend),
usage, and the ServiceNow terminology reference.

Quick start (assumes the backend is already running on port 8001):

```bash
npm install
npm run dev
```

Then open http://localhost:3000.
